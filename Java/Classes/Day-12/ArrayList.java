/*
ARRAY LIST:
    -Dynami (can change there size any time)
    name.add(2) {this will add things in array}
    name.get(index) {indexOf}
    name.remove(index) {delete}
    name.set(index,value) {update}
    name.size() {lenght}
    name.clear() {all clear}
    syantax:
    Arraylist<Integer> name=new Arraylist<>().
*/
// Write a java program to insert your friends name.
